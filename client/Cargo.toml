[package]
name = "motherboard-client"
version = "0.1.0"
edition = "2024"

[dependencies]
libc = "0.2.186"
motherboardm-protocol = { path = "../protocol", features = ["std"]}
tokio = { version = "1.48.0", optional = true, features = ["net"] }

[features]
default = []
tokio = ["dep:tokio"]
